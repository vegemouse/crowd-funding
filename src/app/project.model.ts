export class Project {
  constructor (public title: string, public manager: string, public teaser: string, public description: string, public moneyGoal: number, public moneyRaised: number, public benefits: string, public image: string, public city: string, public daysToGoal: number, public category: string) { }
}
